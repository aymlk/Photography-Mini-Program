# 2026-09-16 工作日志

## 项目目录迁移（两次）

- 第一次：从 `C:\Users\AYMOLK\WorkBuddy\2026-09-06-21-13-10` 整体（robocopy /MOVE /MT:16）迁移到 `D:\aicodeing\摄影小程序`。验证 `npm run build` 通过、preview 5180 正常。
- 第二次（用户纠正目标）：从 `D:\aicodeing\摄影小程序` 再迁移到 **`D:\桌面\aicodeing\摄影小程序`**（注意在 D 盘的"桌面"目录内，不是系统 C 盘桌面）。旧父目录 `D:\aicodeing` 与 C 盘空壳均已清空删除。
- **最终工程根目录**：`D:\桌面\aicodeing\摄影小程序`
  - Web：`D:\桌面\aicodeing\摄影小程序\`（Vite + React + TS，npm run dev/build/preview）
  - 小程序：`D:\桌面\aicodeing\摄影小程序\lumen-mp\`（Taro，构建需 `APPDATA="C:\Users\AYMOLK\AppData\Roaming"`）
- **验证**：`npm run build` 通过（tsc --noEmit + vite build，1944 modules、JS 330.88KB / CSS 30.49KB）；preview 5180 返回 HTTP 200；bundle 命中 `apis.map.qq.com` / `29.8012`（桐庐坐标）/ `chengli_studio` / `hello@chengli.studio`。功能无任何回归。
- ⚠️ 记忆文件现在也在新目录 `.workbuddy/memory/` 下；系统提示里注入的旧 `C:\Users\AYMOLK\WorkBuddy\...` 路径已失效，后续记忆读写一律用 `D:\桌面\aicodeing\摄影小程序\.workbuddy\memory\`。
